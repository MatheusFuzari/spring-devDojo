spring:
  application:
    name: anime-service
  threads:
    virtual:
      enabled: true

logging:
  level:
    com.example.dev_dojo: debug

server:
  port: 8090
  servlet:
    context-path: /
  error:
    include-stacktrace: on_param
  tomcat:
    threads:
      max: 20
